import React from 'react';

const Card = (props) => {
    let {strMealThumb,strArea,strCategory}=props.product
    console.log(strCategory);
    return (
        <div>
      
      <div>
        
      </div>

        </div>
    );
};

export default Card;